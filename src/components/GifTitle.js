import React from "react";

// Define a function that is our component, always make sure to declare the props parameter so you can use props in your component
const GifTitle = (props) => {
    return <h1>Giphy</h1>;
};

export default GifTitle;